<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>pPUSH</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <%= if current_user = admin_user(@conn)do %>
              <li> <%= link "Hello, #{ current_user.email}", to: "" %></li>
              <li> <%= link gettext("Logout"), to: delete_session_path(@conn, :delete) %></li>
            <% end %>
          </ul>
        </nav>
        <span class="logo"></span>
        <a class="link-home" href="/"><span class="first">p</span>PUSH</a>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <%= render "navigation.html", current_user: @current_user, conn: @conn %>


      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
